[project]
name = "conf-crawler"
version = "0.1.0"
description = "CCF conference deadline crawler — DeepSeek v4-flash extraction"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0",
    "httpx>=0.27",
    "rich>=13",
]
